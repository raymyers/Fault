[package]
name = "fault_smt"
version = "0.1.0"
edition = "2024"

[dependencies]
fault_syntax = { path = "../fault_syntax" }
fault_resolve = { path = "../fault_resolve" }
fault_eval = { path = "../fault_eval" }
fault_exec = { path = "../fault_exec" }
fault_temporal = { path = "../fault_temporal" }
